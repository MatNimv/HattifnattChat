import { firebase, firestore, updateDoc, db, doc, getDoc } from './firebaseConf.js';

const getUserEmails = async (uid, sessionId) => {
    try {
        

    } catch (error) {
        console.error('Error updating document: ', error);
    }
};

export default getUserEmails;
